
// array of strings type
const superHeros: string[] = [];
superHeros.push("Thor");

//array of numbers type
const numbers: number[] = [];
numbers.push(1);

const heroPower: Array<string> = [];

//array of objects type
type User = {  
    name: string;
    isActive: boolean;
}

const allUsers: User[] = [];
allUsers.push({name: "sagar", isActive: true});

//multi dimensional array
const MLModels:number[] [] = [
    [1,2,3],
    [4,5,6]
];